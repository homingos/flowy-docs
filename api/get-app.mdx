---
title: "Get an app"
description: "Fetch an app's identity and its input/output schema."
openapi: "/api-reference/openapi.json GET /v1/apps/{appId}"
---

Each input's `name` (its kebab-cased title) is the `node_id` you pass in a run request `inputs` entry. See [Run an app](/api/running-apps) for the full flow.
