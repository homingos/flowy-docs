---
title: "Get a flow"
description: "Fetch a flow's identity and its input/output schema."
openapi: "/api-reference/openapi.json GET /v1/apps/{appId}"
---

Each input's `name` (its kebab-cased title) is the `node_id` you pass in a run request `inputs` entry. See [Run a flow](/api/running-apps) for the full flow.
