---
title: "Get a run"
description: "Poll a run's status and, once complete, its output media URLs."
openapi: "/api-reference/openapi.json GET /v1/runs/{runId}"
---

Output media URLs are signed and expire. Download or copy the asset to your own storage soon after the run completes.
