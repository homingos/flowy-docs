---
title: "List apps"
description: "List the active apps in the authenticated key's workspace."
openapi: "/api-reference/openapi.json GET /v1/apps"
---

List the apps in your workspace and grab the `id` of the one you want to run. Fetch that app's input schema next with [Get an app](/api/get-app).
