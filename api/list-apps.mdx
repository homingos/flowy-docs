---
title: "List flows"
description: "List the active flows in the authenticated key's workspace."
openapi: "/api-reference/openapi.json GET /v1/apps"
---

List the flows in your workspace and grab the `id` of the one you want to run. Fetch that flow's input schema next with [Get a flow](/api/get-app).
